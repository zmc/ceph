meta:
  - desc: "Packaged prometheus and grafana"
overrides:
  cephmetrics:
    group_vars:
      all:
        backend:
          metrics: mgr
          storage: prometheus
        containerized: false
