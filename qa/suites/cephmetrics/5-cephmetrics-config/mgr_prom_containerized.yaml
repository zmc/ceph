meta:
  - desc: "Containerized prometheus and grafana"
overrides:
  cephmetrics:
    group_vars:
      all:
        backend:
          metrics: mgr
          storage: prometheus
        prometheus:
          containerized: true
        grafana:
          containerized: true
